Proceso dosDecimas	
	// Proceso: Parseo de Texto a número
	// Leer encabezado[7];
	// Leer encabezado[8];
	// encabezado[9] <- ConvertirATexto(ConvertirANumero(encabezado[7]) * ConvertirANumero(encabezado[8]));
	// Escribir encabezado[9];
	// Proceso: Dos cifras decimales
	// Leer n;
	// Leer m;
	// res <- n / m;
	// res <- trunc(n*m*100.0)/100.0;
	// Escribir res;
	// Entrada de Datos: Volver al Menú
FinProceso
